class Currency {
    constructor(code, name, rate, countryCode, symbol) {
        this.code = code;
        this.name = name;
        this.rate = rate;
        this.countryCode = countryCode;
        this.symbol = symbol
    }
}

export default Currency;